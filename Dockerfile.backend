# Dockerfile para backend Node.js
FROM node:18-alpine
WORKDIR /app
COPY backend/package*.json ./
RUN npm install --production && npm install --save-dev sequelize-cli
COPY backend/. .
EXPOSE 3001
CMD ["npm", "start"] 